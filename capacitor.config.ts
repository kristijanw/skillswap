import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kristijanmarijic.skillshare',
  appName: 'Skillshare',
  webDir: 'dist'
};

export default config;
